<Window x:Class="VoiceLinkNative.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoiceLink Native v1.0.0" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e" Foreground="#ffffff">
    
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
        <TextBlock Text="VoiceLink Native v1.0.0" FontSize="20" FontWeight="Bold" Foreground="#ffffff" Margin="0,0,20,0"/>
        <TextBlock Text="✅ Build Successful!" FontSize="16" Foreground="#4ade80" Margin="0,10,20,0"/>
        <TextBlock Text="Windows Native Client for VoiceLink" FontSize="14" Foreground="#764ba2" Margin="0,20,20,0"/>
        <TextBlock Text="Features:" FontSize="16" FontWeight="Bold" Foreground="#ffffff" Margin="0,30,20,0"/>
        <TextBlock Text="• Basic WPF interface" FontSize="12" Foreground="#ffffff" Margin="0,10,0,0"/>
        <TextBlock Text="• Window management" FontSize="12" Foreground="#ffffff" Margin="0,10,0,0"/>
        <TextBlock Text="• Menu system" FontSize="12" Foreground="#ffffff" Margin="0,10,0,0"/>
        <TextBlock Text="• Settings and user interface" FontSize="12" Foreground="#ffffff" Margin="0,10,0,0"/>
        <TextBlock Text="Status: Ready for distribution" FontSize="14" FontWeight="Bold" Foreground="#4ade80" Margin="0,10,20,0"/>
    </StackPanel>
</Window>