VoiceLink TestFlight Build 48

What to test:
- Both public servers appear in `Servers`, with names/descriptions instead of IPs.
- `Federated Rooms` shows search + one room list; double tap a room opens details, and VoiceOver actions expose Preview, Join, and Share.
- Locked/Unlocked, Public/Private, and room details match desktop.
- Preview plays start/stop FX, crossfades the stream, and auto-closes after the countdown.
- People in Room shows all users, device info, mute/output state, and live per-user level meters; transient empty payloads should not wipe the list.
- Monitor solos one user’s relay audio; Whisper ducks room playback.
- Room chat, system/bot messages, Markdown links, and message ordering/limits match room settings.
- Room media starts/stops correctly, including when joining while media is already playing.
- Room Actions can hide/show audio controls; Settings stays available in-room.
- `Show Room Relay Debug Details` only shows relay/status text when enabled.
- Native sign-in, WHMCS fallback, 2FA delivery, and silent WHMCS token refresh after reconnect still work.
